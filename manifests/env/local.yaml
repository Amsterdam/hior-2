host: localhost

jobs:
  certificate-init:
    enabled: false

ingress:
  app-external:
    enabled: false
  app-internal:
    className: nginx
    tls: null

secrets:
  certificate:
    enabled: false

network:
  allow-ingress:
    enabled: false
